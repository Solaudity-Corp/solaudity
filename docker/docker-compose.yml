services:
  backend:
    build: ../backend
    ports:
      - "8001:8001"
    volumes:
      - ../data:/data

  frontend:
    build: ../frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
